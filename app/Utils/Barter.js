/* eslint-disable no-param-reassign */
const properties = ['water', 'food', 'medicine', 'ammo'];

const Barber = {
  equivalence: {
    water: 4,
    food: 3,
    medicine: 2,
    ammo: 1,
  },
  properties,
};

module.exports = Barber;
